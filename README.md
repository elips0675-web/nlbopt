# Оптимизация nlb.by

Прогнал сайт через Lighthouse локально — картина совпадает с вашими 46 баллами, причины оценки видны очень чётко. Вот разбор и план по приоритетам.

## Что тормозит мобильную версию

| Проблема | Масштаб |
|----------|---------|
| Изображения (нет lazy-load, не сжаты, не WebP) | ~7 МБ потенциальной экономии |
| JS-бандлы Битрикса (не минифицированы, лишние модули) | ~1,3 МБ, TBT ~1,2 с |
| Блокирующие CSS + Google Fonts | ~1 с задержки первой отрисовки |
| Ответ сервера (TTFB) | ~0,9 с |
| Короткое кэширование статики | 128 ресурсов |

**Конкретика:** на главной 92 изображения, из них 55 за пределами экрана — и ни одного с `loading="lazy"`. Самые тяжёлые: Af_Lecishcha.png (733 КБ), Sl_Kalyarovi_Susvet.jpg (712 КБ), Af_Danilov.jpg (702 КБ), Sl_Kryshtali_pamyaci.jpg (656 КБ). JS: шаблонный бандл template\_...js — 826 КБ, из них 208 КБ не минифицированы и 79% не используется на главной. Плюс грузятся protobuf.js (71 КБ, тоже не минифицирован), dexie, pull.client, rest.client — на публичной странице они не нужны.

## План по приоритету (эффект / трудозатраты)

### 1. Изображения — главный резерв, до +20 баллов

Добавить `loading="lazy"` всем `<img>` ниже первого экрана (в шаблоне Битрикса — правка компонентов слайдера/афиши/новостей). Это одна строка на тег, эффект — минус ~5–7 МБ трафика на старте.

Сконвертировать JPG/PNG в WebP (экономия ~6,8 МБ) и пережать баннеры: никакой картинке в слайдере не нужно быть 700 КБ — 1200px по ширине и качество 80 хватит с запасом.

В Битриксе: включить обработку изображений (resize\_cache, метод CFile::ResizeImageGet с WebP в новых версиях модуля «Главный»), либо отдать это на уровень nginx/CDN.

### 2. Настройки Битрикса — быстрые переключатели, до +10 баллов

В «Настройки → Главный модуль → Оптимизация CSS/JS» включить: минификацию JS и CSS, объединение файлов, перенос JS в конец страницы. Сейчас минификация выключена — отсюда 208 КБ «сырого» шаблонного JS и 71 КБ protobuf.js.

### 3. Убрать лишние модули Битрикса с публички

pull.client, protobuf, dexie, rest.client тянутся на каждую страницу. Если чат/уведомления на фронтенде не используются — отключить подключение этих модулей в шаблоне сайта. Минус ~400 КБ JS и заметная часть TBT.

### 4. Кэширование — и TTFB, и повторные визиты

Включить «Композитный сайт» (автокомпозит) — TTFB ~0,9 с для Битрикса означает, что страница каждый раз собирается заново.

На уровне nginx/Apache: длинный Cache-Control (год) для /upload/, /bitrix/cache/, шрифтов; сейчас аудит ругается на 128 ресурсов с коротким TTL.

### 5. Шрифты и блокирующий CSS (~1 с к FCP)

Google Fonts грузится без `display=swap` — добавить параметр или переехать на self-hosted шрифты (Open Sans + Noto Serif, подключить с `font-display: swap`).

Добавить `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>`.

Критический CSS для первого экрана — инлайн, остальное с `media="print"` → onload-трюк или `rel="preload"`.

### 6. Сторонние виджеты

Jivosite и megavenue.by грузятся сразу. Переведите их на отложенную загрузку (через requestIdleCallback или по первому взаимодействию пользователя, задержка 3–5 с) — визуально ничего не изменится, а main thread освободится.

## Реалистичный прогноз

Пункты 1–2 (изображения + минификация) — самые дешёвые по труду и дадут основной скачок: с **46 до ~65–75**. Добавление кэширования и чистки модулей — к **~80**. CLS у вас и так неплохой (0,12), спецвозможности и SEO в порядке — так что вся работа именно про вес и блокировки.

---

# Файлы-инструкции

Порядок выполнения (по приоритету):

## Быстрый эффект (сделать за 1 день, даст +20-30 баллов)

1. **`03_minification.md`** — включить минификацию JS/CSS (5 мин в админке)
2. **`01_lazy_load_images.md`** — добавить loading="lazy" (копировать код в init.php)
3. **`04_remove_unused_modules.md`** — отключить pull/protobuf/dexie/rest.client (копировать код в init.php)
4. **`08_convert_to_webp.sh`** — запустить конвертацию изображений (залить и запустить скрипт)
5. **`02_webp_and_compress.md`** — настроить Nginx для WebP + сжатие

## Средний приоритет (ещё +10-15 баллов)

6. **`05_composite_and_cache.md`** — включить композит и Cache-Control
7. **`06_fonts_display_swap.md`** — display=swap + preconnect для шрифтов

## Дополнительно

8. **`07_defer_widgets.md`** — отложить загрузку Jivosite и megavenue.by
9. **`09_security_and_ssl.md`** — HTTP-заголовки безопасности (HSTS, CSP, X-Frame-Options и др.), усиление SSL/TLS, rate limiting

---

## Инструменты для проверки

### 🚀 Производительность

| Инструмент | Что даёт | Как использовать |
|-----------|----------|-----------------|
| **PageSpeed Insights** | Lighthouse с серверов Google для моб. и десктоп. Оценки + рекомендации. | Прогнать после каждого изменения, следить за динамикой. |
| **GTmetrix** | Водопад запросов, видео загрузки, рекомендации. Waterfall и Video — визуальная хронология каждого ресурса. | Полезно для поиска узких мест: какие скрипты блокируют отрисовку, где задержки. |
| **WebPageTest** | Самый детальный тест: видео загрузки, водопад, выбор устройства/браузера/региона, 3G-симуляция. | Запускать из США/Европы, смотреть водопад и Suggested Metrics. |
| **Brotli Test** | Проверяет, включено ли сжатие Brotli (на 20-30% эффективнее Gzip). | Если Brotli отключён — включить в Nginx (модуль ngx_brotli). |
| **HTTP/3 Check** | Проверяет поддержку HTTP/3 (QUIC) — протокол быстрее на мобильных и при плохом соединении. | Если нет — настроить Nginx с quic. |
| **Lighthouse CI** | Прогон Lighthouse в CI/CD на каждый коммит. | Не допускать регрессий производительности. |
| **Chrome DevTools (Performance)** | Запись всех событий (отрисовка, скрипты, лейаут) с точностью до мс. | Найти «тяжёлые» функции JS, долгую отрисовку, лишние перерасчёты стилей. |
| **Webpack Bundle Analyzer** | Визуализация состава JS-бандла: какие библиотеки занимают больше всего места. | Если есть сборка — найти и удалить лишние зависимости. |
| **Cloudflare Speed Report** | Разбивка времени: TTFB, TLS-handshake, первый байт. Сравнение с мировыми сайтами. | Быстрый чек, где теряется время на уровне сети. |

### 🛡️ Безопасность (углублённо)

| Инструмент | Что даёт | Как использовать |
|-----------|----------|-----------------|
| **Mozilla Observatory** | Комплексная оценка: проверяет CSP, HSTS, CORS, Referrer-Policy, X-Content-Type-Options и др. Выдаёт балл и шаги по исправлению. | Запустить первым делом. Цель — A+. |
| **SSL Labs** | Глубокий анализ SSL/TLS: версии протоколов, шифры, уязвимости (Heartbleed, ROBOT), цепочка сертификатов. | Цель — A+. Проверить после настройки TLS. |
| **Security Headers** | Проверяет HTTP-заголовки безопасности и ставит оценку. | Цель — A+. Добавить заголовки из `09_security_and_ssl.md`. |
| **OWASP ZAP** | Интерактивный сканер: находит SQL-инъекции, XSS, CSRF и другие уязвимости. Можно запустить как прокси. | Раз в месяц прогонять пассивное сканирование, активное — под контролем. |
| **Snyk** | Проверяет зависимости (CMS, библиотеки, npm-пакеты) на известные CVE-уязвимости. | Подключить в CI/CD для автоматических проверок. |
| **CSP Evaluator** | Проверяет Content-Security-Policy на слабые места (unsafe-inline, unsafe-eval). | Вставить свою CSP — увидеть, какие директивы слишком мягкие. |
| **Sucuri SiteCheck** | Сканирует на малварь, блоклисты, подозрительные редиректы. | Прогонять ежемесячно или после подозрительной активности. |
| **urlscan.io** | Полный скан: все запросы, поддомены, подключённые скрипты. | Найти неожиданные сторонние скрипты или утечки. |
| **VirusTotal** | Проверка домена 70+ антивирусами и блоклистами. | При подозрении на заражение. |
| **REDbot** | Проверяет HTTP-заголовки кэширования (Cache-Control, ETag, Vary). | Настроить кэширование по рекомендациям. |

### 🔍 SEO и техническое состояние

| Инструмент | Что даёт | Как использовать |
|-----------|----------|-----------------|
| **Screaming Frog SEO Spider** | Сканирует сайт как поисковик: битые ссылки, дубликаты мета-тегов, отсутствие H1, проблемы с canonical. | Прогонять после изменения структуры сайта. |
| **Ahrefs Site Audit / Semrush** | Аудит по 100+ параметрам: дублированный контент, скорость, перелинковка, robots.txt, sitemap.xml. | Комплексная альтернатива Screaming Frog. |
| **Google Search Console** | Мобильная удобность, Core Web Vitals от реальных пользователей (не лабораторные). | Смотреть раздел «Core Web Vitals» и «Мобильная удобность». |
| **Mobile-Friendly Test** | Проверяет, считает ли Google страницу мобильно-дружественной. | Убедиться, что нет ошибок «контент шире экрана», «текст слишком мелкий». |
| **W3C Validator** | Валидация HTML/CSS на соответствие стандартам. | Ошибки могут вызывать странное поведение в разных браузерах. |
| **Schema Validator** | Проверка JSON-LD/Microdata/Open Graph разметки. | Битрикс часто генерирует кривую микроразметку — поправить для сниппетов. |
| **Webhint** | Универсальный линтер: безопасность, производительность, доступность, совместимость, SEO — в одном CLI/расширении. | Запускать в CI/CD или разово для общей картины. |
| **JSON-LD Playground** | Визуализация и проверка структурированных данных. | Вставить JSON-LD — увидеть, как его понимают поисковики. |

### ♿ Доступность (a11y) и UX

| Инструмент | Что даёт | Как использовать |
|-----------|----------|-----------------|
| **axe DevTools** | Глубокий анализ a11y по WCAG 2.1. Конкретные указания: какой элемент и как исправить. | Расширение браузера. Прогнать после вёрстки новых блоков. |
| **WAVE** | Визуальная разметка страницы: прямо на странице показывает alt, контраст, aria-атрибуты. | Быстрая проверка без глубоких технических знаний. |
| **Google Lighthouse (Accessibility)** | Балл и список рекомендаций по доступности. | Встроен в DevTools. |
| **Color Contrast Checker** | Проверяет контрастность текста и фона. | Для людей с нарушениями зрения — AA (минимум) / AAA (хорошо). |

### 📊 Мониторинг и нагрузка

| Инструмент | Что даёт | Как использовать |
|-----------|----------|-----------------|
| **k6** | Нагрузочное тестирование: сколько запросов/сек выдерживает сервер, при каких условиях падает скорость. | Написать скрипт, запустить перед релизом. |
| **New Relic / Datadog** | APM: сколько времени занимают запросы к БД, вызовы API, выполнение PHP/Python кода. | Найти узкие места на бэкенде (медленные SQL, внешние вызовы). |
| **UptimeRobot / StatusCake** | Мониторинг доступности и TTFB из разных точек мира. | Уже есть — добавить проверку времени ответа. |
| **Pingdom** | Мониторинг скорости и аптайма с выбором региона. | Альтернатива UptimeRobot с визуальными отчетами. |

### 🧪 Дополнительно

| Инструмент | Что даёт | Как использовать |
|-----------|----------|-----------------|
| **Can I Use** | Проверяет поддержку CSS/JS-фич в целевых браузерах. | Проверить перед использованием новых веб-стандартов. |
| **WebPageTest (повторно)** | Сравнительный тест «до» и «после» оптимизации. | Записать видео загрузки до и после — наглядно видно разницу. |

---

## 💡 Стратегия проверок

1. **Сначала** — Mozilla Observatory + SSL Labs: покажут критические дыры в безопасности.
2. **Затем** — Lighthouse (или GTmetrix) с эмуляцией 4G/3G: понять, где теряются секунды LCP.
3. **Для контроля** — UptimeRobot (уже есть) + Snyk для автоматических проверок зависимостей.
4. **Периодически** — Screaming Frog (SEO), axe DevTools (a11y), OWASP ZAP (безопасность).
5. **После изменений** — PageSpeed Insights для проверки динамики баллов.

---

Для каждого пункта: прочитайте файл, скопируйте код на сервер, примените.
